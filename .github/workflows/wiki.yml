name: Email notifier

on:
  gollum:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    if: "contains(github.gollum.pages[].summary, '[Important]')"
    steps:
      - name: test-env
        run: echo "enviar email"
