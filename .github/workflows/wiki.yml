name: Email notifier

on:
  gollum:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test-env
        run: echo "::warning ${{ github.event.commits[0].message }}"
